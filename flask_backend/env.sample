# Скопируйте этот файл как .env и отредактируйте значения
FLASK_ENV=production
SECRET_KEY=замените_на_случайную_строку_64симв
WTF_CSRF_SECRET_KEY=замените_на_другую_строку_64симв

# Подключение к PostgreSQL (используется внутри docker-compose)
DATABASE_URL=postgresql+psycopg://mich55_db_user:eddad4a2b2fe6e10633f415fc3bee598@db:5432/mich55_db

# Если 1 — backend подключается к Postgres, иначе автоматически переключается на SQLite
USE_REMOTE_DB=1 